import { useEffect, useMemo, useRef, useState } from 'react';
import { Captions, CaptionsOff, LoaderCircle, Radio, RefreshCw, Settings2 } from 'lucide-react';
import { getActiveUserId, getLiveChannelGuide, listLiveChannels, liveChannelStreamUrl, resolveMediaUrl } from '../../../api';
import type { GuideChannel, LiveChannel, MediaItem } from '../../../types';
import { ChannelArtwork } from './ChannelArtwork';
const WINDOW_SECONDS=3*60*60,HALF_HOUR=30*60,MIN_REMAINING_GUIDE=60*60;const cacheKey=()=>`onyx-live-guide:${getActiveUserId()}`;const clock=(timestamp:number)=>new Date(timestamp*1000).toLocaleTimeString([],{hour:'numeric',minute:'2-digit'});const placeholders=(channels:LiveChannel[]):GuideChannel[]=>channels.map(channel=>({channel,current:undefined,programs:[]}));const reviveRows=(rows:GuideChannel[]):GuideChannel[]=>{const now=Math.floor(Date.now()/1000);return rows.map(row=>{const current=row.programs.find(program=>program.startsAt<=now&&program.endsAt>now);return current?{...row,current:{...current,offsetSeconds:Math.max(0,now-current.startsAt)}}:{...row,current:undefined}})};const reviveCachedGuide=():GuideChannel[]=>{try{const raw=localStorage.getItem(cacheKey());return raw?reviveRows(JSON.parse(raw) as GuideChannel[]):[]}catch{return[]}};const guideHasRoom=(rows:GuideChannel[])=>{const now=Math.floor(Date.now()/1000);return rows.length>0&&rows.every(row=>row.programs.length===0||Math.max(...row.programs.map(program=>program.endsAt))>=now+MIN_REMAINING_GUIDE)};const openLibraryItem=(mediaId:string)=>window.dispatchEvent(new CustomEvent('onyx-open-library-item',{detail:{mediaId}}));
export function LiveChannelsView({media,onOpenSettings,projector=false,userName}:{media:MediaItem[];onOpenSettings:()=>void;projector?:boolean;userName?:string}){const cached=useMemo(()=>reviveCachedGuide(),[]);const[guide,setGuide]=useState<GuideChannel[]>(cached);const[tuned,setTuned]=useState<GuideChannel|null>(()=>cached.find(row=>Boolean(row.current))??null);const[initializing,setInitializing]=useState(cached.length===0);const[loadingGuide,setLoadingGuide]=useState(false);const[error,setError]=useState<string|null>(null);const[subtitlesOn,setSubtitlesOn]=useState(false);const[fullscreen,setFullscreen]=useState(Boolean(document.fullscreenElement));const videoRef=useRef<HTMLVideoElement>(null);const videoShellRef=useRef<HTMLDivElement>(null);const channelRefs=useRef(new Map<string,HTMLButtonElement>());const refreshBusy=useRef(false);const lastNetworkRefresh=useRef(0);const initialFocusDone=useRef(false);const mediaById=useMemo(()=>new Map(media.map(item=>[item.id,item])),[media]);const now=Math.floor(Date.now()/1000);const windowStart=Math.floor(now/HALF_HOUR)*HALF_HOUR;const windowEnd=windowStart+WINDOW_SECONDS;const ticks=Array.from({length:7},(_,index)=>windowStart+index*HALF_HOUR);
const refresh=async(autoTune=false)=>{if(refreshBusy.current)return;refreshBusy.current=true;setLoadingGuide(true);try{const request=getLiveChannelGuide();const timeout=new Promise<never>((_,reject)=>window.setTimeout(()=>reject(new Error('Live guide took too long to respond. Try Refresh.')),15_000));const next=reviveRows(await Promise.race([request,timeout]));lastNetworkRefresh.current=Date.now();localStorage.setItem(cacheKey(),JSON.stringify(next));setGuide(next);setError(null);setTuned(current=>{if(!current&&autoTune)return next.find(row=>Boolean(row.current))??null;if(!current)return current;return next.find(row=>row.channel.id===current.channel.id)??next.find(row=>Boolean(row.current))??null})}catch(c){setError(String(c))}finally{refreshBusy.current=false;setLoadingGuide(false);setInitializing(false)}};
useEffect(()=>{let disposed=false;let timer:number|undefined;const start=async()=>{try{if(projector){await refresh(true)}else{const channels=await listLiveChannels();if(disposed)return;const known=new Set(channels.map(channel=>channel.id));const useful=reviveRows(cached.filter(row=>known.has(row.channel.id)));const cacheMatches=useful.length===channels.length&&channels.length>0;setGuide(cacheMatches?useful:placeholders(channels));if(cacheMatches&&!tuned)setTuned(useful.find(row=>Boolean(row.current))??null);setInitializing(false);if(channels.length)await refresh(true)}timer=window.setInterval(()=>{setGuide(current=>{const revived=reviveRows(current);if(!guideHasRoom(revived)||(projector&&Date.now()-lastNetworkRefresh.current>=5*60_000))void refresh(false);return revived});setTuned(current=>current?reviveRows([current])[0]:current)},projector?30_000:60_000)}catch(c){if(!disposed){setError(String(c));setInitializing(false)}}};void start();return()=>{disposed=true;if(timer)window.clearInterval(timer)}},[]);
useEffect(()=>{if(!projector)return;const reload=()=>void refresh(false);const visible=()=>{if(document.visibilityState==='visible')reload()};window.addEventListener('focus',reload);document.addEventListener('visibilitychange',visible);return()=>{window.removeEventListener('focus',reload);document.removeEventListener('visibilitychange',visible)}},[projector]);
useEffect(()=>{if(!projector||initialFocusDone.current||!guide.length)return;initialFocusDone.current=true;requestAnimationFrame(()=>channelRefs.current.get(tuned?.channel.id??guide[0].channel.id)?.focus({preventScroll:true}))},[projector,guide,tuned?.channel.id]);
useEffect(()=>{const changed=()=>setFullscreen(Boolean(document.fullscreenElement));document.addEventListener('fullscreenchange',changed);return()=>document.removeEventListener('fullscreenchange',changed)},[]);
const tune=(row:GuideChannel)=>{const revived=reviveRows([row])[0];if(revived.current){setTuned(revived);setSubtitlesOn(false);return}void refresh(true)};const tunedMedia=tuned?.current?mediaById.get(tuned.current.mediaId):undefined;const stream=tuned?.current?liveChannelStreamUrl(tuned.current.mediaId,tuned.current.offsetSeconds):undefined;const fallbackArt=tunedMedia?.backdropUrl??tunedMedia?.thumbnailUrl??tunedMedia?.posterUrl;const subtitleTracks=tunedMedia?.subtitles.filter(track=>Boolean(track.url))??[];
useEffect(()=>{const video=videoRef.current;if(!video)return;for(let index=0;index<video.textTracks.length;index++)video.textTracks[index].mode=subtitlesOn&&index===0?'showing':'disabled'},[subtitlesOn,tuned?.current?.mediaId]);
const keepLive=()=>{const video=videoRef.current;if(!document.body.classList.contains('sleep-mode')&&video?.paused&&!video.ended)void video.play().catch(()=>{})};const toggleFullscreen=async()=>{try{if(document.fullscreenElement){await document.exitFullscreen();return}await videoShellRef.current?.requestFullscreen()}catch(c){setError(`Could not change fullscreen mode: ${String(c)}`)}};
useEffect(()=>{if(!projector)return;const nav=(event:Event)=>{const command=(event as CustomEvent<string>).detail;if(command==='fullscreen'){void toggleFullscreen();return}if(command==='subtitles'){if(subtitleTracks.length)setSubtitlesOn(v=>!v);return}if(!['up','down','left','right','ok'].includes(command))return;const active=document.activeElement as HTMLElement|null;const buttons=[...channelRefs.current.values()];let index=buttons.indexOf(active as HTMLButtonElement);if(index<0)index=Math.max(0,guide.findIndex(row=>row.channel.id===tuned?.channel.id));if(command==='ok'){buttons[index]?.click();return}const cols=Math.max(1,Math.floor((document.querySelector('.projector-channel-grid')?.clientWidth??1)/(buttons[0]?.offsetWidth||1)));if(command==='left')index--;if(command==='right')index++;if(command==='up')index-=cols;if(command==='down')index+=cols;index=Math.max(0,Math.min(buttons.length-1,index));buttons[index]?.focus({preventScroll:false})};window.addEventListener('onyx-projector-command',nav);return()=>window.removeEventListener('onyx-projector-command',nav)},[projector,guide,tuned?.channel.id,subtitleTracks.length,fullscreen]);
return <div className={`live-page ${projector?'projector-live-page':''}`}><section className="live-header"><div><p className="eyebrow">{projector&&userName?`${userName.toUpperCase()}'S LIVE CHANNELS`:'LIVE CHANNELS'}</p><h1>What’s on now</h1></div><div className="live-header-actions">{loadingGuide&&guide.length>0&&<span className="live-guide-status"><LoaderCircle className="spin" size={14}/>Updating…</span>}<button className="live-icon-button" title="Refresh guide" onClick={()=>void refresh(false)}><RefreshCw size={17}/></button>{!projector&&<button className="live-icon-button" title="Manage channels" onClick={onOpenSettings}><Settings2 size={17}/></button>}</div></section>{error&&<div className="error-banner">{error}</div>}{tuned?.current&&<section className="live-player-card"><div className="live-player-info"><div><span className="live-badge"><i/>LIVE</span><p>{tuned.channel.name}</p><button className="live-library-link" onClick={()=>openLibraryItem(tuned.current!.mediaId)}><h2>{tuned.current.title}</h2>{tuned.current.subtitle&&<span>{tuned.current.subtitle}</span>}</button></div></div><div ref={videoShellRef} className={`live-video ${fullscreen?'is-fullscreen':''}`} onClick={()=>void toggleFullscreen()}><video ref={videoRef} key={`${tuned.channel.id}:${tuned.current.mediaId}:${tuned.current.startsAt}`} autoPlay playsInline src={stream} onPause={keepLive} onLoadedMetadata={keepLive}>{subtitleTracks.map((track,index)=><track key={`${track.label}:${index}`} kind="subtitles" src={resolveMediaUrl(track.url)} srcLang={track.language||'und'} label={track.label||track.language||`Subtitle ${index+1}`}/>)}</video>{subtitleTracks.length>0&&<button className={`live-caption-toggle ${subtitlesOn?'active':''}`} onClick={event=>{event.stopPropagation();setSubtitlesOn(v=>!v)}}>{subtitlesOn?<Captions size={20}/>:<CaptionsOff size={20}/>}</button>}</div></section>}{projector&&guide.length>0&&<section className="projector-channel-section"><h2>Channels</h2><div className="projector-channel-grid">{guide.map(row=>{const currentMedia=row.current?mediaById.get(row.current.mediaId):undefined;const visual=row.channel.artUrl??(!row.channel.artIcon?(currentMedia?.backdropUrl??currentMedia?.thumbnailUrl??currentMedia?.posterUrl):undefined);return <button ref={el=>{if(el)channelRefs.current.set(row.channel.id,el);else channelRefs.current.delete(row.channel.id)}} className={`projector-channel ${tuned?.channel.id===row.channel.id?'tuned':''}`} key={row.channel.id} onClick={()=>tune(row)}>{visual?<img src={resolveMediaUrl(visual)} alt=""/>:<ChannelArtwork channel={row.channel}/>}<span><strong>{row.channel.name}</strong><b>{row.current?.title??'Guide loading…'}</b>{row.current?.subtitle&&<small>{row.current.subtitle}</small>}</span></button>})}</div></section>}{initializing&&guide.length===0?<div className="live-empty"><LoaderCircle className="spin"/><span>Loading channels…</span></div>:guide.length===0?<div className="live-empty"><Radio size={34}/><h2>No channels yet</h2></div>:!projector&&<section className="guide-shell">{guide.map(row=><div className="guide-row" key={row.channel.id}><button className="guide-channel" onClick={()=>tune(row)}><strong>{row.channel.name}</strong></button><div className="guide-timeline">{row.programs.map(program=><button key={`${program.mediaId}:${program.startsAt}`} onClick={()=>tune(row)}>{program.title} {clock(program.startsAt)}</button>)}</div></div>)}</section>}</div>}
