import {useEffect,useMemo,useState}from'react';import{ArrowDown,ArrowLeft,ArrowRight,ArrowUp,ChevronLeft,Pause,Play,RotateCcw,RotateCw,Volume1,Volume2,VolumeX,Moon,Sunrise,Search,Clapperboard,Tv,Film,Radio,Maximize,Captions,RefreshCw}from'lucide-react';import{getRemoteLibrary,getRemoteState,remoteArtworkUrl,sendRemoteCommand,type RemoteCommand,type RemoteMediaItem,type RemoteState}from'./remoteApi';const tokenFromPath=()=>decodeURIComponent(location.pathname.match(/^\/remote\/([^/]+)/)?.[1]??'');const time=(s=0)=>{const h=Math.floor(s/3600),m=Math.floor(s%3600/60),sec=Math.floor(s%60);return h?`${h}:${String(m).padStart(2,'0')}:${String(sec).padStart(2,'0')}`:`${m}:${String(sec).padStart(2,'0')}`};export function isPhoneRemoteRoute(){return!!tokenFromPath()}type Tab='remote'|'home'|'movies'|'tv'|'specials';const art=(i:RemoteMediaItem,ep=false)=>remoteArtworkUrl(ep?(i.thumbnailUrl||i.backdropUrl||i.posterUrl):(i.posterUrl||i.thumbnailUrl||i.backdropUrl));export function RemotePage(){const token=tokenFromPath();const[state,setState]=useState<RemoteState|null>(null);const[library,setLibrary]=useState<RemoteMediaItem[]>([]);const[error,setError]=useState('');const[tab,setTab]=useState<Tab>('remote');const[query,setQuery]=useState('');const[selectedShow,setSelectedShow]=useState('');const refresh=async()=>{try{setState(await getRemoteState(token));setError('')}catch(c){setError(String(c))}};useEffect(()=>{void refresh();const t=setInterval(()=>void refresh(),500);return()=>clearInterval(t)},[token]);useEffect(()=>{void getRemoteLibrary().then(setLibrary).catch(c=>setError(String(c)))},[]);const command=async(v:RemoteCommand)=>{try{setState(await sendRemoteCommand(token,v));setError('')}catch(c){setError(String(c))}};const playing=state?.nowPlaying;const[current,setCurrent]=useState(0);useEffect(()=>{if(playing?.currentTime!=null)setCurrent(playing.currentTime)},[playing?.currentTime]);useEffect(()=>{if(!playing||playing.paused)return;const t=setInterval(()=>setCurrent(v=>Math.min(playing.duration??Infinity,v+.25)),250);return()=>clearInterval(t)},[playing?.paused,playing?.duration]);const movies=useMemo(()=>library.filter(i=>i.kind==='movie'),[library]),episodes=useMemo(()=>library.filter(i=>i.kind==='episode'),[library]),specials=useMemo(()=>library.filter(i=>i.kind==='special'),[library]);const shows=useMemo(()=>{const m=new Map<string,RemoteMediaItem>();episodes.forEach(i=>{const n=i.showTitle||i.title;if(!m.has(n))m.set(n,i)});return[...m.entries()]},[episodes]);const selectedEpisodes=useMemo(()=>episodes.filter(i=>(i.showTitle||i.title)===selectedShow).sort((a,b)=>(a.season??0)-(b.season??0)||(a.episode??0)-(b.episode??0)),[episodes,selectedShow]);const seasons=useMemo(()=>{const m=new Map<number,RemoteMediaItem[]>();selectedEpisodes.forEach(i=>m.set(i.season??0,[...(m.get(i.season??0)??[]),i]));return[...m.entries()].sort((a,b)=>a[0]-b[0])},[selectedEpisodes]);const cards=(items:RemoteMediaItem[])=><div className="remote-library-grid">{items.map(i=><button className="remote-media-card" key={i.id} onClick={()=>void command(`play:${i.id}`)}>{art(i)?<img src={art(i)} alt=""/>:<Film/>}<strong>{i.title}</strong></button>)}</div>;const episodeList=<>{seasons.map(([season,items])=><section className="remote-season" key={season}><h2>{season?`Season ${season}`:'Specials'}</h2>{items.map(i=><button className="remote-episode-row" key={i.id} onClick={()=>void command(`play:${i.id}`)}>{art(i,true)?<img src={art(i,true)} alt=""/>:<span className="remote-episode-placeholder"><Tv/></span>}<span><b>{i.episode!=null?`${i.episode}. `:''}{i.title}</b><small>{i.durationSeconds?time(i.durationSeconds):'Length unavailable'}</small></span></button>)}</section>)}</>;const libraryView=<section className="remote-library"><div className="remote-search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search Onyx"/></div>{tab==='tv'?(selectedShow?<><button className="remote-library-back" onClick={()=>setSelectedShow('')}><ChevronLeft/>All shows</button><h1>{selectedShow}</h1>{episodeList}</>:<><h2>TV Shows</h2><div className="remote-library-grid">{shows.map(([name,item])=><button className="remote-media-card" key={name} onClick={()=>setSelectedShow(name)}>{art(item)?<img src={art(item)} alt=""/>:<Tv/>}<strong>{name}</strong></button>)}</div></>):query?cards(library.filter(i=>`${i.title} ${i.showTitle||''}`.toLowerCase().includes(query.toLowerCase()))):tab==='movies'?cards(movies):tab==='specials'?cards(specials):cards(library.filter(i=>i.progressSeconds>0).slice(0,20))}</section>;return <main className="phone-remote"><header><span className="remote-brand">ONYX</span><span className={`remote-status ${state?.projectorConnected?'online':''}`}>{state?.projectorConnected?'Projector connected':'Disconnected'}</span><button className="remote-reconnect" onClick={()=>void refresh()} title="Reconnect"><RefreshCw size={15}/></button></header>{error&&<div className="remote-error">{error}<button onClick={()=>void refresh()}><RefreshCw/>Reconnect now</button></div>}{tab==='remote'?<><section className="remote-now"><p>NOW PLAYING</p>{playing?.artwork&&<img src={playing.artwork}/>}<div><h1>{playing?.title??'Nothing playing'}</h1>{playing?.subtitle&&<h2>{playing.subtitle}</h2>}{playing?.duration&&<><div className="remote-progress"><i style={{width:`${Math.min(100,current/playing.duration*100)}%`}}/></div><small>{time(current)} / {time(playing.duration)} · {Math.max(0,Math.round((playing.duration-current)/60))} min remaining</small></>}</div></section><section className="remote-transport"><button onClick={()=>void command('seekBack')}><RotateCcw/><span>10</span></button><button className="remote-play" onClick={()=>void command('playPause')}>{playing?.paused?<Play/>:<Pause/>}</button><button onClick={()=>void command('seekForward')}><RotateCw/><span>10</span></button></section><section className="remote-player-tools"><button onClick={()=>void command('fullscreen')}><Maximize/>Fullscreen</button><div className="remote-subtitle-select"><Captions/><select value={playing?.subtitles?.find(s=>s.active)?.index??-1} disabled={!playing?.subtitles?.length} onChange={e=>{const n=Number(e.target.value);void command(n<0?'subtitles':`subtitle:${n}`)}}><option value={-1}>Subtitles off</option>{playing?.subtitles?.map(s=><option value={s.index} key={s.index}>{s.label}{s.language?` (${s.language})`:''}</option>)}</select></div></section><section className="remote-pad"><button className="up" onClick={()=>void command('up')}><ArrowUp/></button><button className="left" onClick={()=>void command('left')}><ArrowLeft/></button><button className="ok" onClick={()=>void command('ok')}>OK</button><button className="right" onClick={()=>void command('right')}><ArrowRight/></button><button className="down" onClick={()=>void command('down')}><ArrowDown/></button></section><section className="remote-volume"><h3>Volume</h3><div className="remote-volume-three"><button onClick={()=>void command('volumeDown')}><Volume1/>−</button><button className={playing?.muted?'active':''} onClick={()=>void command('mute')}><VolumeX/>Mute</button><button onClick={()=>void command('volumeUp')}><Volume2/>+</button></div></section><section className="remote-sleep"><h3>Sleep Timer</h3><div><button onClick={()=>void command('sleepNow')}><Moon/>Now</button><button onClick={()=>void command('sleep30')}>30m</button><button onClick={()=>void command('sleep60')}>1h</button><button onClick={()=>void command('sleep120')}>2h</button>{state?.sleeping&&<button onClick={()=>void command('wake')}><Sunrise/>Wake</button>}</div></section></>:libraryView}<nav className="remote-tabs"><button onClick={()=>setTab('remote')}><Radio/><span>Remote</span></button><button onClick={()=>setTab('home')}><Clapperboard/><span>Browse</span></button><button onClick={()=>setTab('movies')}><Film/><span>Movies</span></button><button onClick={()=>setTab('tv')}><Tv/><span>TV</span></button><button onClick={()=>setTab('specials')}><Clapperboard/><span>Specials</span></button></nav></main>}
